import React from 'react';
import Form from './Form';

const FormContainer = (props) => {
  console.log(props);
  return (
    <>
    <Form formFunction />
    </>
  )
}

export default FormContainer;